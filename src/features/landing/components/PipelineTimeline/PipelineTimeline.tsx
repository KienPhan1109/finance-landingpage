import type { PipelineTimelineProps } from "../../types";
import "./PipelineTimeline.css";

export function PipelineTimeline(_props?: PipelineTimelineProps) {
  return (
    <section
      className="pipeline-section"
      id="pipeline-section"
      aria-label="Scroll Section"
    />
  );
}

